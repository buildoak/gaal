# Archived Skill Note

This file used to contain an older internal Gaal skill draft. The public skill
now lives in `skill/SKILL.md`; use that file as the authoritative agent guide.

The old draft intentionally is not preserved here because public skill bundles
should not ship machine-specific paths, private operational doctrine, or stale
command syntax. Useful public command facts have been folded into
`skill/SKILL.md` and `skill/references/`.
